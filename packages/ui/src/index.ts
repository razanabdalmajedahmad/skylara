/**
 * SkyLara shared UI — foundation components (Phase 3).
 * Add domain-specific components in apps/web or extend here when reused by multiple apps.
 */
export { SKYLARA_BRAND } from './tokens';
export { Spinner, type SpinnerProps } from './spinner';
export { PageLoading, type PageLoadingProps } from './page-loading';
export { PageEmpty, type PageEmptyProps } from './page-empty';
export { PageError, type PageErrorProps } from './page-error';
